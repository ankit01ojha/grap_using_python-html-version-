# grap_using_python-html-version
First run ```python graph.py```.
This will create a ```svg file``` , add the location of the svg file in ```chart.html```.
Then open the ```HTML file``` in web-browser.
